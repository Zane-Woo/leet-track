# 题迹体验评测

评测日期：2026-08-03  
评测范围：首页、题库、题目详情、刷题记录、统计、设置、备份恢复、PWA 离线行为。

## 结论

题迹的视觉方向清晰：纸张底色、深色进度卡和橙色强调共同形成了稳定的「个人刷题手账」气质。核心任务路径已经顺畅——进入题库、打勾、填写本次记录、再次点击撤销。此次评测主要补齐了数据安全、移动端触控、深色语义色和离线边界。

## 多尺寸与主题检查

| 场景 | 结果 | 证据 |
| --- | --- | --- |
| 375×812 移动端 | 通过；无横向溢出，主要控件均达到 44px | [首页](screenshots/review-v2-home-mobile-375.png)、[题库](screenshots/review-v2-library-mobile-375.png) |
| 768×1024 平板 | 通过；内容宽度和卡片层级稳定 | [截图](screenshots/review-v2-home-tablet-768.png) |
| 1280×800 桌面 | 通过；主内容保持可读宽度 | [截图](screenshots/review-v2-home-desktop-1280.png) |
| 深色模式 | 通过；进度主卡保持深色而非反转成亮色 | [截图](screenshots/review-v2-home-dark-mode-mobile-375.png) |

## 已修复问题

### 必须修复

- 备份导入过去只做浅层判断，异常 ID、日期、范围或重复记录可能污染本地数据。现已加入完整结构校验，并只保留仍存在于题库中的收藏与记录。
- 清空必填日期后提交会触发无效日期转换并可能导致页面崩溃。现已先验证本地日期，同时保留浏览器原生必填校验。
- Service Worker 会把缺失的脚本、图片或跨域请求错误地回退为首页 HTML。现仅对同源页面导航使用离线壳，静态资源失败会正确返回错误。
- 自动化测试仍是脚手架示例，无法覆盖真实产品。现已改为题库、备份解析、日期边界、元数据和离线策略测试。

### 应当修复

- 页面标题曾重复显示品牌名，现统一为「题迹 · 力扣刷题记录」。
- 深色模式下主进度卡会被语义反转成浅色，现使用独立的 hero 色彩 token。
- 搜索、筛选、收藏、勾选、弹窗按钮等触控区域不足 44px，现已统一扩大。
- 弹窗缺少完整对话框语义和 Esc 关闭支持，现已补齐焦点、角色、标签和键盘关闭。
- 外观与筛选按钮缺少选中状态语义，现使用 `aria-pressed`。
- iOS 下载备份时使用未挂载锚点并立即释放 URL，存在偶发失败风险；现已挂载后触发并延迟释放。

## 交互回归

- 从题库打开题目、勾选、保存一次刷题记录：通过。
- 再次点击已勾选题目，取消撤销后状态保持：通过。
- 确认撤销后勾选消失：通过。
- 使用 Esc 关闭记录编辑器：通过。
- 空日期提交不会崩溃：通过。
- 117 道题的编号、slug、难度、顺序和必填字段完整，未发现重复：通过。

## 后续观察项

- 当前数据坚持本地优先；跨设备同步不是缺陷，但换机前必须导出备份。若以后加入账号体系，应优先设计冲突合并而不是简单覆盖。
- 力扣题单内容若发生变化，需要同步更新内置题库版本，并为迁移增加专门测试。

